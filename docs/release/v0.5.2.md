# v0.5.2 リリース受入契約

Release readiness: BLOCKED

## 要件・検証対応表

| 要件 / 失敗モード | ロードマップ / Issue | 実装箇所 | 独立した検証 | 実行結果・証跡 | 状態 |
| --- | --- | --- | --- | --- | --- |
| Zstd 設定が wire 変換だけで終わり、送受信経路で復元されない | #1 / v0.5.2 | `chirps-file-transfer` compression / send / receive | compression・FileTransfer integration tests | workspace test と FileTransfer acceptance | 確認済み |
| chunk が破損しても NACK/retry されず、完了として扱われる | #1 / v0.5.2 | chunk stream codec / receive handler | 実 QUIC corruption-proxy integration | FileTransfer integration test | 確認済み（loopback relay） |
| receiver の最終 hash / atomic rename 前に Complete が返る | #1 / v0.5.2 | receive finalization | FileTransfer integration | FileTransfer acceptance | 確認済み |
| 中断後の resume が低速・OS差でタイムアウトし、検証済みチャンクを復帰できない | #27 / v0.5.2 | resumable session / cancel / resume | FileTransfer integration (全OS CI) | 256 KiB・64 KiB/sの決定的フィクスチャへ修正。WSL統合24件中23件成功（1件は意図したignore） | CI確認中 |
| 家庭内 LAN / `iperf3` / WSL の値を Chirps の product throughput と誤認する | #1 / v0.5.2 | `ft-1g-v1` controlled two-container harness | 二 container、固定veth qdisc、tmpfs、5 sample、SHA/image/profile manifest | 固定WSL2プロファイル（CPU 0-3/4-7）で5件全て109 MB/s以上。swap制御は別属性として記録 | 確認済み |
| 最終 binary 試験まで FileTransfer の性能原因を局所化できない | #25 / v0.5.2 | machine-readable performance contract、function Criterion、module invariants、実 QUIC service/binary report | contract wiring UT、phase/concurrency/retry accounting、same-host Criterion comparator | source manifest 151.42→135.90 ms、resume checkpoint 61.85→16.56 ms、WSL product phase・memory evidenceを取得 | 確認済み |
| 物理二 host の UDP 到達性を確認せず QUIC 配備失敗を実装性能と誤認する | #1 / v0.5.2 | `PATH-UDP-100` deployment diagnostic | 双方向 UDP 100 Mbit/s / 15 s / loss 0% と実 FileTransfer 完全性 | two-host deployment artifact | 診断専用（release performance 非 gate） |
| 依存 advisory が job 成功として扱われ、release gate を通過する | #13 / v0.5.2 | `security-audit` CI job | `cargo audit` の終了コード | cargo-audit脆弱性0、CI run 30885314021 Security Audit成功 | 確認済み |
| macOS の有効な temporary source path を traversal と誤認して FileTransfer が失敗する | #14 / v0.5.2 | FileTransfer path validation | `macos-latest` FileTransfer integration | canonical path比較へ修正。CI run 30885314021 macOS stable成功 | 確認済み |
| Windows で TLS/QUIC dependency を含む workspace が compile できない | #15 / v0.5.2 | Windows MSVC / `aws-lc-sys` dependency path | `windows-latest` workspace test | canonical path比較へ修正。CI run 30885314021 Windows stable成功 | 確認済み |
| tag を持つ操作者だけで crates.io / GitHub Release まで進めてしまう | #16 / v0.5.2 | GitHub `release` environment | environment protection の repository API 確認 | `release` environmentを作成し、required reviewer `asopitech`、prevent self-reviewを設定 | 確認済み |
| `MeshHandle` から FileTransfer を構築できない | #6 / v0.6 | public Mesh API | v0.6 API acceptance | v0.6 milestone | v0.5.2 のスコープ外 |

## 未証明・除外事項

| 項目 | なぜこの版で証明できないか | 追跡 Issue / milestone | 公開可否への影響 |
| --- | --- | --- | --- |
| `ft-1g-v1` controlled two-container で、5 回の 128 MiB FileTransfer が全て E2E >= 100 MB/s | 固定WSL2プロファイル（CPU 0-3 / 4-7）で80df81fを5回測定。109.35–112.80 MB/s（中央値111.37 MB/s）、整合性・メモリ残留もPASS。swap制御不可は別属性として記録 | #1 / v0.5.2 | 確認済み（全体リリースは他ゲート待ち） |
| function/module/service/binary の phase evidence を対応付けた performance 回帰検知 | `performance-contract.json` と local harness で WSL2 二コンテナ製品経路の phase・メモリピーク・作業後残留を取得。固定CPUプロファイルで5回の製品測定も完了 | #25 / v0.5.2 | 確認済み |
| 物理 network 上で application chunk payload を改竄した復旧 | 三ホスト relay は配備診断の追加環境であり、v0.5.2のloopback corruption-proxy検証を代替しない | #1 / v0.5.2 | 診断専用（release blockerではない） |

## 変更影響レビュー

- 公開 API: FileTransfer の直接構築 API は検証可能。`MeshHandle` 統合 API は v0.6 に明示的に分離した。
- wire / persistence: compression、manifest、chunk retry、receiver finalization を integration test で確認した。
- 性能: 100 MB/s は固定した `ft-1g-v1` 二 container profile だけで判定する。物理二 host の `iperf3` と FileTransfer は配備診断であり、家庭内 LAN の帯域を release performance gate にしない。#25 の phase evidence は原因帰属と local 回帰検知に使い、SLO の代替にはしない。
- メモリ安全性: WSLでも複数チャンネル反復転送のセッション解放とcgroup `memory.peak` / 作業後 `memory.current` を測定する。swap controllerの有無は環境適格性と分離し、実測メモリ安全性を無効化しない。
- rollback: 公開前に controlled profile、platform compatibility、または release environment gate が失敗した場合はタグから publish へ進めない。

## 承認

- 実装者: 未記入
- 検証者（実装者以外）: 未記入
- release captain: 未記入
- 対象 commit SHA: 未確定
- Evidence artifact / CI run URL: 未取得
