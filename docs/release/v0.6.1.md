# v0.6.1 リリース受入契約

Release readiness: BLOCKED

この文書は、ロードマップ・Issue・実装・検証・証跡を同じ release candidate に固定するための契約です。実装前に作成し、要求を追加・変更した PR では同じ PR で更新します。`未証明`、`BLOCKED`、`TODO` が残る限り `READY` にしてはなりません。

## 要件・検証対応表

| 要件 / 失敗モード | ロードマップ / Issue | モデル property / 対象外根拠 | 実装箇所 | local 検証 | 実機・外部 evidence | 状態 |
| --- | --- | --- | --- | --- | --- | --- |
| default/no-default feature matrix が警告なしで検査できる | v0.6.1 patch | dead-code回帰をfeature組合せで検証 | `crates/alopex-chirps/src/raft/{node,transport}.rs` | `RUSTFLAGS=-Dwarnings cargo check --locked -p alopex-chirps --no-default-features` / default | PR CI artifact | 未証明 |
| v0.6.0後に追加した代表機能デモが再実行できる | v0.6.1 patch | 論理3ノードread/writeの整合性 | `scripts/demo/v0_6`, `tools/chirps-v06-demo` | `python3 scripts/demo/v0_6/demo.py --scenario all --writes 6` | `docs/demo/results/v0.6.0/2026-08-09/` | 未証明 |

各行は「機能がある」ではなく、壊れた場合に利用者が観測する失敗モードで書きます。並行・分散要件には model property を、対象外には理由を必ず記します。mock-only、loopback-only、手作業確認だけの場合は、その制約を evidence 欄に明記します。

## 未証明・除外事項

| 項目 | なぜこの版で証明できないか | 追跡 Issue / milestone | 公開可否への影響 |
| --- | --- | --- | --- |
| 物理3ノード性能・実ネットワーク障害 | この環境は論理3ノード/MockNetworkのみ | 次版の物理環境Issue | 対象外（性能主張をしない） |

「後で確認する」は除外理由になりません。公開を妨げない項目は、仕様上のスコープ外である根拠と次版の Issue を併記します。

## 変更影響レビュー

- 公開 API / wire format / 永続化 / 設定 / observability への影響: feature compile gate修正のみ。wire/永続形式変更なし。
- 既存テストで覆えない経路: 物理3ノードと実ネットワークは対象外。
- 互換性・移行・rollback: v0.6.0からのpatch upgrade。タグは不変。
- セキュリティ・性能・実ネットワークの確認: Security Audit、全feature、デモ結果を公開前に再確認する。

## 承認

- 実装者:
- 検証者（実装者以外）:
- release captain:
- 対象 commit SHA:
- Evidence artifact / CI run URL:
