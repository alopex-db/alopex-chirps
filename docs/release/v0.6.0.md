# v0.6.0 リリース受入契約

Release readiness: BLOCKED

この契約は v0.6.0 の開始時点の要求台帳である。各 Issue の実装前に、利用者に見える失敗モード、状態モデル（該当時）、production refinement、最小 local 検証を確定する。CI や release workflow を要求漏れの発見場所にはしない。

## 要件・検証対応表

| 要件 / 失敗モード | ロードマップ / Issue | モデル property / 対象外根拠 | 実装箇所 | local 検証 | 実機・外部 evidence | 状態 |
| --- | --- | --- | --- | --- | --- | --- |
| 同じ network seed が異なるイベント列となり、障害を再現できない | #3 / v0.6.0 | network schedule・clock・link fault の状態モデルを実装前に追加 | 未着手 | seed と最小 event schedule を出力する harness test | 対象外。deterministic local harness が対象 | 未証明 |
| crash / WAL / snapshot 破損後に不正な状態を有効として復元する | #4 / v0.6.0 | durable recovery state（write / fsync / rename / restart）のモデルを実装前に追加 | 未着手 | disk fixture の corruption / restart test | 実ディスク fixture の結果を保存 | 未証明 |
| leader 喪失、partition、構成変更後に committed state が分岐する | #5 / v0.6.0 | Raft safety property（commit / term / membership / convergence）を実装前に追加 | 未着手 | deterministic harness 上の external oracle | 必要なら multi-process evidence。条件は Issue で確定 | 未証明 |
| SWIM の一時疑義が Raft voter を不安全に削除し、再参加が収束しない | #6 / v0.6.0 | membership / incarnation / voter safety の結合モデルを実装前に追加 | 未着手 | public API の deterministic + UDP component test | 実 UDP evidence は Issue #6 の受入条件で定義 | 未証明 |
| 複合 QUIC traffic で control traffic が starvation し、queue / memory が無制限に増大する | #7 / v0.6.0 | QoS queue / priority / backpressure state のモデルを実装前に追加 | 未着手 | deterministic traffic と metrics oracle | 実機条件は Issue #7 の設計時に定義 | 未証明 |
| `MeshHandle` から FileTransfer service を構築できず、内部型へ依存する | #17 / v0.6.0 | 既存 `formal/file-transfer` の protocol state を利用。公開 API wiring 自体は状態遷移を増やさない | `alopex-chirps` public API | MeshHandle のみで構築する component test と既存 FileTransfer contract | 物理性能は対象外 | 未証明 |
| 過去版固有の release gate が対象版の publish 可否を決める | #18 / v0.6.0 | version → contract → evidence gate の有限解決規則を実装前に定義 | release workflow / contract verifier | contract-to-gate 解決の local test | CI run URL。物理測定は contract に記載された場合だけ必要 | 未証明 |

## 未証明・除外事項

| 項目 | なぜこの版で証明できないか | 追跡 Issue / milestone | 公開可否への影響 |
| --- | --- | --- | --- |
| v0.6.0 の全要件 | プロジェクト開始時点。各 Issue のモデル、実装、local verification、evidence が未着手 | #3–#7, #17, #18 / v0.6.0 | BLOCKED |
| v0.5.2 の二物理 host 1 Gbps FileTransfer 測定 | v0.5.2 固有の契約であり、v0.6.0 の完了根拠に流用しない | #1 / v0.5.2 | v0.6.0 の対象外。#18 で versioned gate を分離 |

## 変更影響レビュー

- 公開 API / wire format / 永続化 / 設定 / observability への影響: 各 Issue で実装開始前に記入する。
- 既存テストで覆えない経路: deterministic network、実ディスク crash/restart、public Mesh API、複合 QUIC traffic、versioned release gates。
- 互換性・移行・rollback: public API と durable format を変更する Issue は移行・rollback を受入条件に追加する。
- セキュリティ・性能・実ネットワークの確認: #7 と #18 が、対象版固有の条件と evidence を定義する。

## 承認

- 実装者: 未記入
- 検証者（実装者以外）: 未記入
- release captain: 未記入
- 対象 commit SHA: 未確定
- Evidence artifact / CI run URL: 未取得
