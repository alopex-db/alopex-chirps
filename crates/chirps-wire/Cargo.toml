[package]
name = "alopex-chirps-wire"
version.workspace = true
edition = "2024"
description = "Wire protocol types for Alopex Chirps cluster communication"
license.workspace = true
repository.workspace = true
homepage.workspace = true
categories.workspace = true
keywords = ["networking", "protocol", "serialization", "alopex", "chirps"]

[features]
default = []
hlc = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
uuid = { version = "1.8.0", features = ["v4"] }
