[package]
name = "chirps-core"
version = "0.1.0"
edition = "2024"

[dependencies]
chirps-wire = { path = "../chirps-wire" }
async-trait = "0.1"
tokio = { version = "1.35", features = ["sync"] }
thiserror = "1.0"
