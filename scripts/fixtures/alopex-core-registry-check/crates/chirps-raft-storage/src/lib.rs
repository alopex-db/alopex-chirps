// Placeholder used only while generating the isolated fixture lock. The
// verifier replaces this directory with the production crate sources before
// every build and rejects a divergent production/fixture Cargo.toml.
